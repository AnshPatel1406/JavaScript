see the Image

there we will return the reference of the function by lexical scope 

if we dont do that inner function , then we directly call the function , not return it 

calling it will change the background colour withouth clicking the button because we are calling the function 